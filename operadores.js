const [a, b, c, d] = [2, 7, 14, 9]

const soma = a + b + c + d
const subtracao = a - b -c - d
const multiplicacao = a * b 
const divisao = c / b
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)
